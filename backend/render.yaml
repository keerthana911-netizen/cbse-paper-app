services:
  - type: web
    name: cbse-paper-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python -m app.seed"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: CBSE_APP_SECRET_KEY
        generateValue: true
